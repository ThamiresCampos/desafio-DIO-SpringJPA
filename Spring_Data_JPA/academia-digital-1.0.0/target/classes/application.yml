/Spring:
  datasource:
    url: jdbc:postgresql://localhost5432/academia?useTimezone=true&serverTimezone=UTC&useLegacyDate
    username: root
    password: root

jpa:
  show-sql: true
  hibernate:
    ddl-auto: update
    properties:
      hibernate.format_sql: true